namespace Progs_Ing.Matrices;

public class ContadorNumprimos
{
    public ContadorNumprimos()
    {/*
        int[] numeros = {6,35,22,29,70,44,13,15,24,67};
        int contadorPrimos = 0;
        
        Console.WriteLine("Analizando números primos:");
        foreach (int n in numeros)
        {
            if (EsPrimo(n))
            {
                Console.WriteLine($"{n} es primo");
                contadorPrimos++;
            }
            else
            {
                Console.WriteLine($"{n} no es primo");
            }
        }
        
        Console.WriteLine($"Total de números primos: {contadorPrimos}");
    }

    private static bool EsPrimo(int n)
    {
        if (n <= 1) return false;
        if (n == 2) return true;
        if (n % 2 == 0) return false;
        
        for (int i = 3; i * i <= n; i += 2)
        {
            if (n % i == 0) return false;
        }
        return true;
        */
    }

    private static void Primo()
    {
    }


}